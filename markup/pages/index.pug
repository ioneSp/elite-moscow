include ../components/libs/bemto/bemto
include ../components/libs/jade-injected/jade-injected

include ../components/head/head.pug
include ../components/header/header
include ../components/social/social
include ../components/slider/slider
include ../components/block-container/block-container
include ../components/footer/footer.pug

doctype html
html(class="no-js", lang="ru")
    head
        +head(head.defaults)
    body.page
        | %=symbols=%
        +header()
        +slider({
            type: ''
        })
        +b.section
            +e.box
                +block-container({
                    title: 'Популярные Бренды',
                    headerButtons: [
                        {
                            name: 'Все стили',
                            link: '#'
                        },
                        {
                            name: 'Классика',
                            link: '#'
                        },
                        {
                            name: 'Современный',
                            link: '#'
                        }
                    ]
                })
        +slider({
            type: 'main',
            elements: []
        })

        section.page__wrapper
            +social({mod: 'arrow'})
            +social({mod: 'search'})
            +social({mod: 'basket'})
            +social({mod: 'home'})
            +social({mod: 'user'})
            +social({mod: 'phone'})
            +social({mod: 'burger'})
            +social({mod: 'map'})
            +social({mod: 'phone-two'})
            +social({mod: 'social-vk'})
            +social({mod: 'social-fb'})
            +social({mod: 'social-in'})
            +social({mod: 'social-lin'})
            +social({mod: 'social-yt'})
            +social({mod: 'social-in'})
            +social({mod: 'close'})
            +social({mod: 'search-main'})
            +social({mod: 'filter'})

            +b.section
                +e.box

        .page__footer
            +footer()

        script(src='%=static=%js/separate-js/svg4everybody.min.js')
        script svg4everybody();

        //- Main scripts. You can replace it, but I recommend you to leave it here
        script(src='%=static=%js/main%=hash=%%=min=%.js')
